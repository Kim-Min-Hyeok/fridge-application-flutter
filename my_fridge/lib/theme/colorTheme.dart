import 'package:flutter/material.dart';

class ColorStyle {
  static const primary = Color.fromRGBO(25, 63, 128, 100);
  static const secondary = Color.fromRGBO(143, 76, 60, 100);
  static const background = Colors.white;
} 